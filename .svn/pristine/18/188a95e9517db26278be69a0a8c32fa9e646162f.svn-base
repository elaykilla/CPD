#{extends 'Dendrites/template.html' /} 
#{set title:'Dendrite - ' + dendrite?.named + 'Bureau'/}

<!--<div class="container">-->
<div class="container-fluid span9" style="text-align: justify;">
    <div class="headline">  
        <h2>Le Bureau</h2>
    #{if currentMandates.isEmpty()}
    <h3 style="margin-bottom: 40px"> Cette dendrite n'a pas encore de Bureau. Veuillez organiser des Elections. </h3>
    #{/if }
    #{else}
    </div>
    <h3 style="margin-bottom: 40px">Le Bureau de la dendrite de ${currentMandates?.first()?.dendrite?.named} est élu annuellement. Elle est constituée par les membres suivants:
    </h3>

    <ul class="thumbnails team">
        #{list items:currentMandates, as:'mandate'}
        #{membreBureauTag mandate:mandate /}
        #{/list}
    </ul>
    #{/else}
</div>