
<form id="form-cv" class="form-cv form-horizontal" method="POST" action="@{Cvs.updateCv}">
    <div id="ajaxable">
        <fieldset>
            <legend>Les informations du CV</legend>
            <input type="hidden" name="cv.id" value="${_cv?.id}"/>
            <input type="hidden" name="act" value="cv"/>
            #{field 'cv.title'}
            <div
                class="control-group">
                <label class="control-label" for="${field.id}">&{'Titre'}</label>
                <div class="controls">
                    <input class="span10" id="${field.id}" type="text" name="${field.name}"
                           value="${_cv?.title}" placeholder="&{'Titre'}" required /> 
                </div>
            </div>
            #{/field}
            #{field 'cv.resume'}
            <div
                class="control-group">
                <label class="control-label" for="${field.id}">&{'Résumé'}</label>
                <div class="controls">
                    <textarea class="span10" id="${field.id}" type="text" name="${field.name}"
                              placeholder="Résumé" required>${_cv?.resume}</textarea> 
                </div>
            </div>
            #{/field}
            <div class="form-actions">
                <input type="submit" value="&{'Valider'}" class="btn-u" />
            </div>
        </fieldset>
    </div>
</form>
<div class="cachable-group">
    <span class="cachable-button"><div class="cv-title cv">Formations</div><hr class="line"/></span>
    <div class="cachable">
        #{list items:_cv?.formations, as:'formation' }
        <div class="cachable-group">
            <span class="cachable-button btn-u">${formation.school}</span>
            <div class="cachable">
                #{formationEditTag cv:_cv, formation:formation, years:_years, months:_months /}
            </div>
        </div>
        #{/list}
        <span class="cachable-button btn-u">Ajouter une formation</span>
        <div class="cachable">
            #{formationEditTag cv:_cv, years:_years, months:_months /}
        </div>
    </div>
</div>

<div class="cachable-group">
    <span class="cachable-button"><div class="cv-title cv">Expériences</div><hr /></span>
    <div class="cachable">
        #{list items:_cv?.experiences, as:'experience'}
        <div class="cachable-group">
            <span class="cachable-button btn-u">${experience.company}</span>
            <div class="cachable">
                #{experienceEditTag cv:_cv, experience:experience, years:_years, months:_months /}
            </div>
        </div>
        #{/list}
        <span class="cachable-button btn-u">Ajouter une expérience</span>
        <div class="cachable">
            #{experienceEditTag cv:_cv, years:_years, months:_months /}
        </div>
    </div>
</div>

<div class="cachable-group">
    <span class="cachable-button"><div class="cv-title cv">Compétences</div><hr /></span>
    <div class="cachable">
        #{list items:_cv?.skills, as:'skill'}
        <div class="cachable-group">
            <span class="cachable-button btn-u">${skill.domain}</span>
            <div class="cachable">
                #{skillEditTag cv:_cv, skill:skill, years:_years, months:_months /}
            </div>
        </div>
        #{/list}
        <span class="cachable-button btn-u">Ajouter une compétence</span>
        <div class="cachable">
            #{skillEditTag cv:_cv /}
        </div>
    </div>
</div>

<div class="cachable-group">
    <span class="cachable-button"><div class="cv-title cv">Centres d'intérêt</div><hr /></span>
    <div class="cachable">
        #{list items:_cv?.hobbies, as:'hobbie'}
        <div class="cachable-group">
            <span class="cachable-button btn-u">${hobbie.title}</span>
            <div class="cachable">
                #{hobbieEditTag cv:_cv, hobbie:hobbie, years:_years, months:_months /}
            </div>
        </div>
        #{/list}
        <span class="cachable-button btn-u">Ajouter un centre d'intérêt</span>
        <div class="cachable">
            #{hobbieEditTag cv:_cv /}
        </div>
    </div>
</div>

<div class="cachable-group">
    <span class="cachable-button"><div class="cv-title cv">Langues</div><hr /></span>
    <div class="cachable">
        #{list items:_cv?.languages, as:'language'}
        <div class="cachable-group">
            <span class="cachable-button btn-u">${language.language}</span>
            <div class="cachable">
                #{languageEditTag cv:_cv, language:language, languages:_languages, levels:_levels /}
            </div>
        </div>
        #{/list}

        <span class="cachable-button btn-u">Ajouter une langue</span>
        <div class="cachable">
            #{languageEditTag cv:_cv, languages:_languages, levels:_levels /}
        </div>
    </div>
</div>
