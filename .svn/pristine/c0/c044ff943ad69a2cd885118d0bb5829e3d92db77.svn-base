#{extends 'Dendrites/template.html' /} 
#{set title:'Dendrites' /}


<div class="container-fluid span12" style="text-align: justify;" >
    <div class="row-fluid">
        <div class="row span12">
            <h3> Retrouver ici les differentes dendrites de la Cellule. Les Dendrite sont des entités de la CPD. Elles representent des groupes de CPDiens
                vivant dans diverses villes à travers la France. La Dendrite originale est celle de Valence. 

                Chaque utilisateur appartient à une Dendrite. Il peut alors organiser des evenements, activites etc au sein de ça dendrite. 
            </h3> 

            <div class="row span12"> 
                #{if dendrites.size()}
                #{list items:dendrites, as:'d'}
                <a href="@{Dendrites.dendrite(d?.id)}">
                    <div class="span6" style="margin-left: 0px; margin-right: 10px;">
                        <h2 class="color-green pull-left" style="color:blue; font-size:30px; margin-left: 40px;">${d?.named}</h2>
                        <!--<h2 class="titles">${id?.named}</h2>-->
                        <div class="view view-tenth span12"> 
                            <img

                                #{if d?.named == 'Valence'}
                                src="@{'/public/images/dendrites/valence.jpg'}" 
                                #{/ if}

                                #{if d?.named == 'Grenoble'}
                                src="@{'/public/images/dendrites/grenoble.jpg'}" 
                                #{/ if}

                                #{if d?.named == 'Paris'}
                                src="@{'/public/images/dendrites/paris.jpg'}" 
                                #{/ if}

                                #{if d?.named == 'Angers'}
                                src="@{'/public/images/dendrites/angers.jpg'}" 
                                #{/ if}

                                #{if d?.named == 'Lyon'}
                                src="@{'/public/images/dendrites/lyon.jpg'}" 
                                #{/ if}

                                #{if d?.named == 'Mali'}
                                src="@{'/public/images/dendrites/mali.jpg'}" 
                                #{/ if}

                                #{if d?.named == 'CPD'}
                                src="@{'/public/images/dendrites/cpd.jpg'}" 
                                #{/ if}

                                alt="" />
                                <div class="mask">
                                <h2>${d.named}</h2>
                                #{if d?.photo}
                                <p>${d?.photo?.description} </p>
                                #{/if}
                                #{else}
                                <p>${d?.description} </p>
                                #{/else}
                                <a href="@{Dendrites.dendrite(d?.id)}" class="info">Acceder</a>
                                <div class="caption">
                                    #{if connected?.canEdit(d)}
                                    <p align="center"><a href="@{Administration.editDendrite(d?.id)}" class="btn btn-u">Modifier</a></p>
                                    #{/if }
                                </div>
                            </div>
                        </div>
                    </div>
                </a>
                #{/list}
                #{/if}
                #{else}
                <p>Aucune dendrite.</p>
                #{/else}
            </div>
        </div>
        <!-- /container -->
    </div>
</div>
<!--[if lt IE 9]>
    <script src="assets/js/respond.js"></script>
<![endif]-->
